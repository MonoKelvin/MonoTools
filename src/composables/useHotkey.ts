// 热键管理（暂时未使用，后续实现）
export function useHotkey() {
  // TODO: 实现热键管理
  return {
    registerGlobal: async () => {},
    unregisterGlobal: async () => {},
    registerLocal: () => () => {},
    unregisterLocal: () => {},
  }
}
